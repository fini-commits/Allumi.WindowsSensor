name: Build Windows Sensor (Squirrel)

on:
  workflow_dispatch: {}

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Debug - Show Update folder contents
        shell: pwsh
        run: |
          Write-Host "Update folder contents:"
          Get-ChildItem -Path Update -Recurse | Format-Table -AutoSize

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Find project file
        id: proj
        shell: pwsh
        run: |
          $proj = Get-ChildItem -Recurse -Filter *.csproj | Select-Object -First 1
          if (-not $proj) { Write-Error "No .csproj found"; exit 1 }
          Write-Host "Found project: $($proj.FullName)"
          echo "path=$($proj.FullName)" >> $env:GITHUB_OUTPUT

      - name: Restore
        run: dotnet restore "${{ steps.proj.outputs.path }}"

      - name: Clean
        run: dotnet clean "${{ steps.proj.outputs.path }}" -c Release

      - name: Publish single-file exe
        run: >
          dotnet publish "${{ steps.proj.outputs.path }}"
          -c Release -r win-x64
          -p:PublishSingleFile=true
          -p:IncludeNativeLibrariesForSelfExtract=true
          -o publish

      - name: Show publish output
        shell: pwsh
        run: |
          Write-Host "Publish directory contents:"
          Get-ChildItem -Recurse publish | Format-Table -AutoSize

      - name: Install Squirrel tool from NuGet
        shell: pwsh
        run: |
          # Download squirrel.exe from the Clowd.Squirrel package
          $toolPath = "tools"
          New-Item -ItemType Directory -Path $toolPath -Force | Out-Null
          dotnet tool install --tool-path $toolPath Clowd.Squirrel
          Write-Host "Squirrel tool installed to: $toolPath"
          Get-ChildItem $toolPath

      - name: Find published exe
        id: exe
        shell: pwsh
        run: |
          $exe = Get-ChildItem publish -Filter *.exe | Select-Object -First 1
          if (-not $exe) { Write-Error "No EXE found in publish/"; exit 1 }
          Write-Host "Found exe: $($exe.FullName)"
          echo "path=$($exe.FullName)" >> $env:GITHUB_OUTPUT

      - name: Releasify with Clowd.Squirrel
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path dist -Force | Out-Null
          & tools\squirrel pack --packId "AllrumiWindowsSensor" --packVersion "1.0.52" --packDir publish --releaseDir dist

      - name: Upload Squirrel artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-sensor-squirrel
          path: dist/**
